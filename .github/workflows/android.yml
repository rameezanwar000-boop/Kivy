name: Build Kivy APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Install system dependencies (complete autotools + ffi fix)
      run: |
        sudo apt update
        sudo apt install -y \
          automake \
          autoconf \
          libtool \
          libtool-bin \
          autopoint \
          m4 \
          pkg-config \
          build-essential \
          clang \
          cmake \
          libffi-dev \
          python3-pip python3-setuptools python3-venv \
          zip unzip

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"

    - name: Install Buildozer
      run: |
        pip install --upgrade pip
        pip install buildozer cython virtualenv

    - name: Build APK
      # Keep this fix here, it is necessary.
      env:
        ACLOCAL_PATH: /usr/share/aclocal
      run: |
        # --- CRITICAL CHANGE: Uncomment the clean command ---
        # This wipes the previous failed state so the fix can work.
        buildozer android clean 
        
        buildozer android debug

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Kivy-APK
        path: bin/*.apk
